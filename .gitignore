# ------------------------------------------------------------
# Instructor-only material must NEVER be committed to this
# public repo. The reaction-game solution and answer key live
# in the separate instructor bundle.
# ------------------------------------------------------------
*SOLUTION*
*solution*
*Solution*
*INSTRUCTOR*
*instructor*
*Instructor*
*answer_key*
*ANSWER_KEY*
*answer-key*

# Wi-Fi credentials (session 2+) -- each bench makes its own from
# secrets_TEMPLATE.py; real credentials never enter the repo
secrets.py

# OS / editor noise
.DS_Store
Thumbs.db
__pycache__/
*.pyc
.vscode/

# Test/browser artifacts
.playwright-mcp/
scoreboard_data.json
*.png
!sessions/*/diagrams/*.png
