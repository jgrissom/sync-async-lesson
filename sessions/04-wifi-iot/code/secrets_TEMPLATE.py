# ============================================================
# COPY this file to a new file named  secrets.py  and fill in
# your values. Then upload secrets.py to the board.
#
# secrets.py is GITIGNORED on purpose: real network credentials
# must never be committed to a public repo. This template is the
# only version that lives in git.
# ============================================================

WIFI_SSID = "YourNetworkName"
WIFI_PASSWORD = ""        # "" (empty) for an open / registered guest network

# The class scoreboard -- hosted in the cloud. These are the normal
# values; change them ONLY if the instructor announces a fallback.
SCOREBOARD_HOST = "false-start.azurewebsites.net"
SCOREBOARD_PORT = 80

# Your bench number ("1" through "13") -- one per student, it's how
# the leaderboard tells your board's results apart. Ask if unsure.
BENCH = "1"
